<?xml version='1.0' standalone='yes'?>
<!DOCTYPE PLUGIN [
<!ENTITY name "NBDExport">
<!ENTITY author "RifleJock">
<!--
  Versioning — Unraid plugin updates use strcmp() (lexicographic), NOT version_compare.
  No hyphens. Bump only this entity; asset ?v= uses it.

  Rules (ibigsnet fleet — StorageGuard / Thunderbolt Net / FabricRouting / NBD Export):
    YYYY.MM.DD       first release that calendar day
    YYYY.MM.DDaa     2nd same day, then ab, ac, ... az, ba, bb, ...
  Two-letter only after the bare date — never single a–z
  (strcmp treats "aa" as older than "z").
  Calendar day = lab wall clock (not invented “tomorrow”). See RELEASES.md.

  Product: **NBD Export** (UI Network Services → **NBD**). Plugin id **NBDExport**.
  PluginURL file: nbd.plg (not install.plg / nbdexport.plg).

  UI: Network Settings siblings via ibigsGotoNetTab(Title) — never deep-link
  /Settings/ThunderboltNet or /Settings/FabricRouting (standalone CA pages).
-->
<!ENTITY version "2026.08.14ay">
<!ENTITY launch "Settings/NBDExport">
<!ENTITY pluginURL "https://raw.githubusercontent.com/ibigsnet/NBDExport/main/nbd.plg">
<!ENTITY github "ibigsnet/NBDExport">
<!ENTITY raw "https://raw.githubusercontent.com/ibigsnet/NBDExport/main">
<!ENTITY forum "https://forums.unraid.net/topic/200219-plugin-nbd-export-host-disks-over-network-block-device-image-to-qcow2raw/">
<!ENTITY plugdir "/usr/local/emhttp/plugins/NBDExport">
<!ENTITY configdir "/boot/config/plugins/NBDExport">
]>

<PLUGIN name="&name;"
  author="&author;"
  version="&version;"
  launch="&launch;"
  pluginURL="&pluginURL;"
  icon="database"
  min="6.12.0"
  support="&forum;"
  project="https://github.com/&github;"
  readme="https://raw.githubusercontent.com/&github;/stable/README.md">

<CHANGES>
###&version;
- **Host:** multi-network bind via checkboxes (one <code>qemu-nbd</code> per selected IP, same disk/port).

###2026.08.14ax
- **Docs:** public README/docs hygiene — no lab hostnames; private notes stay local.

###2026.08.14aw
- **Pull:** warn (does not block) when output extension mismatches format
  (e.g. qcow2 + <code>.img</code>, raw + <code>.qcow2</code>); live hint + confirm.

###2026.08.14av
- **UD overlay:** no empty reserved slots on unhosted disks (stock layout restored);
  badges only on active Hosts, absolutely positioned to avoid column reflow.

###2026.08.14au
- **UD overlay:** fixed-width disk-row NBD RO/RW slots (no table flicker/shift on UD
  refresh); update in place instead of remove/re-add.

###2026.08.14at
- **UI:** live status updates badges in place (Active→Failed/Stopped) without full page
  reload — Stop/Cancel controls stay usable while jobs finish or die.

###2026.08.14as
- **UI:** writable caveat banner only when Destructive is Off and RW hosts remain;
  emergency stop button stacked under the text (not inline).

###2026.08.14ar
- **Fix:** auto-refresh on Pull fail / Host stop — server snapshot + Status tab watcher (was missing). qemu-img deaths show <strong>Failed</strong> not Idle.

###2026.08.14aq
- **UD overlay:** reliable Main → Unassigned Devices integration — disk-row
  <strong>NBD RO/RW</strong> pills plus an <strong>NBD Hosts</strong> panel under
  SMB/NFS/ISO shares (local Host exports, not mounts). Opt-in still required.

###2026.08.14ap
- **Security UX:** emergency <strong>Stop all writable hosts</strong> (and Stop all) on every tab
  while RW is listening. Destructive Off still allows already-running writable Hosts
  (documented caveat); settings flash warns if RW is still up.

###2026.08.14ao
- **UI:** auto-refresh NBD pages when a Host goes Stopped/stale or a Pull job finishes /
  fails (including qemu-img errors that used to look “Idle”). Polls live status while
  exports/jobs are active.

###2026.08.14an
- **Fix:** Unassigned Devices badges only annotated the first disk row (JS row-dedupe bug);
  <strong>NBD RO</strong>/<strong>NBD RW</strong> now appear on the correct hosted device
  (e.g. Dev 2 / nvme1n1).

###2026.08.14am
- **Opt-in Unassigned Devices badges:** Settings → show small <strong>NBD RO</strong> /
  <strong>NBD RW</strong> lettering on Main → Unassigned Devices for hosted disks.
  Default Off. Best-effort DOM overlay (UD owns that page). See
  <code>docs/integration-unassigned-devices.md</code>.

###2026.08.14al
- **Security docs:** expanded <code>SECURITY.md</code> (CA review / threat model),
  <code>docs/hosting-safety.md</code> Host checklist, bind isolation guide.

###2026.08.14ak
- **UI:** host export blue badge is <strong>Active</strong> (was “Starting…”) so a live
  process is not mistaken for still waiting to begin.

###2026.08.14aj
- **Scan fix:** private <strong>routes</strong> (not only local iface prefixes), optional
  <code>scan_extra_subnets</code>, and remembered peer IPs for cross-subnet discovery.

###2026.08.14ai
- **Discovery:** Pull tab <strong>Scan network</strong> — private LAN probe for NBD ports (10809+) and
  peer beacons (10808). Host auto-starts a private-only JSON beacon while exports are up.
- Docs: <code>docs/discovery.md</code>, <code>docs/client-attach.md</code> (live VM / nbd-client patterns).

###2026.08.14ah
- **Support:** Unraid forum topic is the primary support URL (Plugins / CA Support).
- **Fix:** ship non-empty <code>DOCS.md</code> (empty blob on main caused <code>plugin install</code> zero-length failure).

###2026.08.14ag
- **Rename:** product **NBD Export**, plugin id **NBDExport**, PluginURL `nbd.plg` (was NbdExport / install.plg).
- UI menu title stays **NBD** under Network Services. CA Name: NBD Export.
- Migrate flash/emhttp from legacy `NbdExport` paths on install; remove old plg basenames.


###2026.08.14ab
- **Lab channel:** On branch `main`, PluginURL + raw FILE sources point at `main` (lab uninstall/reinstall testing).
- Branch `stable` remains the CA/production pin (see RELEASE-CHANNEL.md). Ship by merging to stable and rewriting entities to stable.

###2026.08.14aa
- **License:** GNU GPLv3 or later (copyright ibigs, LLC; Author: RifleJock).
- **Release channel:** PluginURL + raw sources on branch `stable` (install.plg remains Latest URL).
- SECURITY.md: RO default, no bind-all, destructive off, managed-pid uninstall.

###2026.08.13ae
- Fleet standard ibigsGotoNetTab (same as af).

###2026.08.13ad
- Companion Network Settings tab links; `install.plg` as Latest after empty CDN on bare `nbdexport.plg`.

###2026.08.13ac
- Companion tab links; pluginURL `?v=` attempt.

###2026.08.13ab
- Companion tab links; restore DOCS after empty-file mishap.

###2026.08.13aa
- Companion tab-link fix; plg body restored.

###2026.08.13
- (superseded) companion tab-link fix.

###2026.08.11bm
- UI: drop redundant hosted-disks lead line; empty state is enough.
- UI: remove meta “Always visible” note from hosted-disks header; public copy only.
- Docs: Destructive mode wording — NBD block protocol, not imaging-only; mounted case less qcow2-centric.
- Docs/UI: Destructive case 2 = array/parity/cache/pools; case 4 = Unraid boot device (USB or disk holding /boot).
- Docs: destructive-mode.md — four Host cases only, bullet points.
- Docs: NBD is not qcow2-only — raw/.img and other qemu-img targets; formats table.
- Docs: one NVMe slot scenario — prepare image on Unraid, write new drive in a dock, swap once.
- Docs: restore how-to-use.md (full scenarios A–F; prior empty file on main).
- Docs: Wi‑Fi balance — solid private Wi‑Fi OK for smaller jobs; single stream + sparse qcow2 + re-Pull while host stays up; still ordinary TCP (no special resume).
- Docs: when-to-use — common scenarios (laptop→VM, gaming PC→array, recovery, reverse to physical).
- Docs: when-to-use opener — local physical disk feel, over the network.
- Docs: rename “If you click this…” to “What each control does”.
- Docs: Contents/TOC on large pages (how-to-use, DOCS, destructive-mode, …).
- Docs: destructive-mode.md — explicit when to enable Destructive mode.
- Docs/UI: spell out read-only (not RO); table entry not row — avoid RO/row confusion.
- Docs: Scenario E — clear BTRFS snapshot recipe only (public tone).
- Docs: quirky Thunderbolt vs 10G speed notes (Thunderbolt 4 ~20G each way ≈ 2× 10G NIC) in how-to / when-to / integration.
- Docs: Scenario E — cold physical-disk qcow2 on Unraid + honest BTRFS snapshot versioning.
- Docs: Scenario C wording — Host where disk plugs in (may lack free space); Pull qcow2 onto roomy Unraid/array.
- Docs: Host wording — raw blocks visible over the network.
- Docs: disambiguate Thunderbolt — Thunderbolt vs multi-terabyte (spell out both).
- Docs: Scenario C expanded — Host NVMe on easy-access Unraid, Pull qcow2 on the box with space.
- Docs: how-to-use tables rewritten for left→right reading; UI map matches tabs (not old section 3/4).
- Wording: Destructive confirm clarifies writable = peer writes selected Unraid disk; array/mounted/flash = host in-use/critical devices.
- Shared chrome: hosted disks + orange Destructive banner on every tab; NBD blurb footer; CLI dropdown on Status.
- Tabs: header-only parent (no blank first tab, like Network Settings/SMB); center tab strip; text tabs.
- UI tabs (Unraid xmenu): Status · Host · Pull · Settings — like Network Settings / SMB strip.
- Multi-disk hosting documented (already supported; free port prefill); CLI under Status as collapsible.

###2026.08.11ak
- Pull output placeholder: `/mnt/user/domains/disk.qcow2` (Unraid VM share default) instead of `/mnt/cache/images/…`.

###2026.08.11aj
- Export config: download JSON or save under /boot/config/ (outside plugin dir); short note that uninstall wipes plugin flash state; optional import from path.
- Keeps auto last-used + named presets on flash as before.

###2026.08.11ai
- Section titles: host local disk/partition on network vs pull remote NBD into a file under /mnt.

###2026.08.11ah
- Section 2 colored empty state + status legend; remember last host/pull fields; named presets save/restore on flash.

###2026.08.11ag
- Wording: section 3 host local Unraid disk/partition on network; section 4 pull remote hosted disk into a file under /mnt.

###2026.08.11af
- Uninstall: stop managed pids only (no global pkill qemu-nbd); clear /var/log/nbdexport and case-variant paths.

###2026.08.11ae
- UI: numbered sections with full-width dividers; action bars under each block so listener vs image job are visually separate.

###2026.08.11ad
- UX/docs: “Start NBD listener” vs image job (server/client); how-to-use guide with scenarios; clearer helpers and status wording.

###2026.08.11ac
- Safety: Destructive mode (default Off) like Unassigned Devices — blocks writable and array/mounted/flash exports; UI confirms; image jobs refuse /dev output.

###2026.08.11ab
- UI: layout unified with FabricRouting/Thunderbolt Net (dl/dt/dd, inline_help, companion strip, status table, Apply/Done).

###2026.08.11aa
- Docs/UI: Unraid product min 6.12 vs qemu tools (not kernel); clearer Status missing-tool text.

###2026.08.11
- Initial public release: Network Services → NBD, RO qemu-nbd export, image job (nbd:// → qcow2/raw), bind IP picker (Thunderbolt first), docs (when to use, vs NFS/SMB, security).
- Soft companions: Thunderbolt Net (prefer Thunderbolt bind), FabricRouting (docs only). Clean uninstall stops exports; does not touch network.cfg.
</CHANGES>

<!--
  NBD Export is intentionally separate from ThunderboltNet / FabricRouting:
  no PHP requires across plugins. NBD is not controlled by network-extra.
-->

<FILE Run="/bin/bash">
<INLINE><![CDATA[
echo "NBD Export: preparing directories..."
# Legacy NbdExport → NBDExport flash migration
if [ -d /boot/config/plugins/NbdExport ]; then
  mkdir -p /boot/config/plugins/NBDExport
  cp -a /boot/config/plugins/NbdExport/. /boot/config/plugins/NBDExport/ 2>/dev/null || true
  if [ -f /boot/config/plugins/NBDExport/NbdExport.cfg ] && [ ! -f /boot/config/plugins/NBDExport/NBDExport.cfg ]; then
    mv /boot/config/plugins/NBDExport/NbdExport.cfg /boot/config/plugins/NBDExport/NBDExport.cfg
  fi
  rm -rf /boot/config/plugins/NbdExport
  echo "NBD Export: migrated flash config from NbdExport → NBDExport"
fi
# Drop old plugin basenames so Plugins tab is not install.plg / dual entries
rm -f /boot/config/plugins/install.plg /boot/config/plugins/nbdexport.plg \
      /var/log/plugins/install.plg /var/log/plugins/nbdexport.plg 2>/dev/null || true
rm -rf /usr/local/emhttp/plugins/NBDExport
rm -rf /usr/local/emhttp/plugins/NbdExport
rm -rf /usr/local/emhttp/plugins/nbdexport
mkdir -p /usr/local/emhttp/plugins/NBDExport/include
mkdir -p /usr/local/emhttp/plugins/NBDExport/scripts
mkdir -p /usr/local/emhttp/plugins/NBDExport/event
mkdir -p /usr/local/emhttp/plugins/NBDExport/docs
mkdir -p /boot/config/plugins/NBDExport
mkdir -p /var/run/nbdexport
mkdir -p /var/log/nbdexport
echo "NBD Export: directories ready."
]]></INLINE>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/NBDExport.page">
<URL>&raw;/NBDExport.page?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/NBDStatus.page">
<URL>&raw;/NBDStatus.page?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/NBDHost.page">
<URL>&raw;/NBDHost.page?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/NBDPull.page">
<URL>&raw;/NBDPull.page?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/NBDSettings.page">
<URL>&raw;/NBDSettings.page?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/default.cfg">
<URL>&raw;/default.cfg?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/README.md">
<INLINE>
<![CDATA[
**NBD Export**

Export a disk or partition as a **Network Block Device** (NBD) and image remote NBD targets to qcow2/raw on Unraid. Lives under **Settings → Network Services → NBD** next to NFS and SMB. Prefer private binds (Thunderbolt Net IPs when available). Read-only by default. Standalone — Thunderbolt Net and FRR are optional companions.
]]>
</INLINE>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/include/nbd-lib.php">
<URL>&raw;/include/nbd-lib.php?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/include/nbd-update.php">
<URL>&raw;/include/nbd-update.php?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/include/nbd-download-config.php">
<URL>&raw;/include/nbd-download-config.php?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/include/nbd-page-boot.php">
<URL>&raw;/include/nbd-page-boot.php?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/include/nbd-page-js.php">
<URL>&raw;/include/nbd-page-js.php?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/include/nbd-beacon-server.php">
<URL>&raw;/include/nbd-beacon-server.php?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/include/nbd-scan.php">
<URL>&raw;/include/nbd-scan.php?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/include/nbd-ud-head.php">
<URL>&raw;/include/nbd-ud-head.php?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/include/nbd-ud-status.php">
<URL>&raw;/include/nbd-ud-status.php?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/include/nbd-live-status.php">
<URL>&raw;/include/nbd-live-status.php?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/include/nbd-live-watch.php">
<URL>&raw;/include/nbd-live-watch.php?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/include/nbd-ud-overlay.js">
<URL>&raw;/include/nbd-ud-overlay.js?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/docs/discovery.md">
<URL>&raw;/docs/discovery.md?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/docs/integration-unassigned-devices.md">
<URL>&raw;/docs/integration-unassigned-devices.md?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/docs/client-attach.md">
<URL>&raw;/docs/client-attach.md?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/scripts/nbd-export-start">
<URL>&raw;/scripts/nbd-export-start?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/scripts/nbd-export-stop">
<URL>&raw;/scripts/nbd-export-stop?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/scripts/nbd-image-from-url">
<URL>&raw;/scripts/nbd-image-from-url?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/event/started">
<URL>&raw;/event/started?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/nbd.plg">
<URL>&raw;/nbd.plg?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/DOCS.md">
<URL>&raw;/DOCS.md?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/SECURITY.md">
<URL>&raw;/SECURITY.md?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/docs/hosting-safety.md">
<URL>&raw;/docs/hosting-safety.md?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/docs/how-to-use.md">
<URL>&raw;/docs/how-to-use.md?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/docs/when-to-use-nbd.md">
<URL>&raw;/docs/when-to-use-nbd.md?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/docs/nbd-vs-nfs-smb.md">
<URL>&raw;/docs/nbd-vs-nfs-smb.md?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/docs/security-and-bind.md">
<URL>&raw;/docs/security-and-bind.md?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/docs/destructive-mode.md">
<URL>&raw;/docs/destructive-mode.md?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/docs/imaging-workflow.md">
<URL>&raw;/docs/imaging-workflow.md?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/docs/integration-thunderboltnet.md">
<URL>&raw;/docs/integration-thunderboltnet.md?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/docs/integration-fabricrouting.md">
<URL>&raw;/docs/integration-fabricrouting.md?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/docs/settings-reference.md">
<URL>&raw;/docs/settings-reference.md?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/docs/troubleshooting.md">
<URL>&raw;/docs/troubleshooting.md?v=&version;</URL>
</FILE>

<FILE Name="/usr/local/emhttp/plugins/NBDExport/docs/README.md">
<URL>&raw;/docs/README.md?v=&version;</URL>
</FILE>

<FILE Run="/bin/bash">
<INLINE><![CDATA[
echo "NBD Export: finishing install..."
chmod 755 /usr/local/emhttp/plugins/NBDExport/scripts/nbd-export-start 2>/dev/null || true
chmod 755 /usr/local/emhttp/plugins/NBDExport/scripts/nbd-export-stop 2>/dev/null || true
chmod 755 /usr/local/emhttp/plugins/NBDExport/scripts/nbd-image-from-url 2>/dev/null || true
chmod 755 /usr/local/emhttp/plugins/NBDExport/event/started 2>/dev/null || true
# Array-start hook
if [ -d /usr/local/emhttp/plugins ]; then
  mkdir -p /usr/local/emhttp/plugins/NBDExport/event
fi
# Unraid event symlink style (if present)
if [ -d /usr/local/emhttp/plugins/dynamix/event ]; then
  :
fi
# Install array-start event if Unraid plugin event path exists
EVDIR=/usr/local/emhttp/plugins/NBDExport/event
if [ -f "$EVDIR/started" ]; then
  chmod 755 "$EVDIR/started"
fi
# Also copy into boot event if using plugin event convention
mkdir -p /boot/config/plugins/NBDExport
if [ ! -f /boot/config/plugins/NBDExport/NBDExport.cfg ]; then
  if [ -f /usr/local/emhttp/plugins/NBDExport/default.cfg ]; then
    cp -a /usr/local/emhttp/plugins/NBDExport/default.cfg /boot/config/plugins/NBDExport/NBDExport.cfg
    echo "NBD Export: created default NBDExport.cfg"
  fi
fi
# Soft-link event into Unraid's plugin event system when available
if [ -d /usr/local/emhttp/plugins ]; then
  # Many plugins register via /boot/config/plugins/*/event or plg FILE — emhttp event/started is enough if plugin manager links it
  :
fi
php -r 'require_once "/usr/local/emhttp/plugins/NBDExport/include/nbd-lib.php"; nbd_write_companion_marker(); nbd_ud_overlay_inject();' 2>/dev/null || true
echo "NBD Export: install complete."
echo "NBD Export: Settings → Network Services → NBD"
echo "NBD Export: Hard-refresh the browser (Ctrl+Shift+R)."
]]></INLINE>
</FILE>

<FILE Run="/bin/bash" Method="remove">
<INLINE><![CDATA[
echo "NBD Export: removing plugin (full cleanup)..."

# 0) Soft head hook for opt-in Unassigned Devices badges
if [ -f /usr/local/emhttp/plugins/NBDExport/include/nbd-lib.php ]; then
  php -r 'require_once "/usr/local/emhttp/plugins/NBDExport/include/nbd-lib.php"; nbd_ud_overlay_uninject();' 2>/dev/null || true
fi
for f in \
  /usr/local/emhttp/webGui/include/DefaultPageLayout/HeadInlineJS.php \
  /usr/local/emhttp/plugins/dynamix/include/DefaultPageLayout/HeadInlineJS.php \
  /usr/local/emhttp/webGui/include/HeadInclude.php \
  /usr/local/emhttp/plugins/dynamix/include/HeadInclude.php
do
  [ -f "$f" ] || continue
  sed -i '/NBDExport UD overlay/d' "$f" 2>/dev/null || true
done

# 1) Stop managed listeners/jobs only (via state files — not global pkill qemu-nbd)
if [ -f /usr/local/emhttp/plugins/NBDExport/include/nbd-lib.php ]; then
  php -r '
    require_once "/usr/local/emhttp/plugins/NBDExport/include/nbd-lib.php";
    nbd_stop_all_exports();
    foreach (nbd_jobs_state() as $j) {
      if (!empty($j["id"])) { nbd_image_stop($j["id"]); }
    }
  ' 2>/dev/null || true
fi
# Best-effort: kill only pids recorded under our run dir
if [ -d /var/run/nbdexport ]; then
  for pf in /var/run/nbdexport/*.pid; do
    [ -f "$pf" ] || continue
    pid=$(tr -cd '0-9' <"$pf" 2>/dev/null || true)
    if [ -n "$pid" ] && [ -d "/proc/$pid" ]; then
      kill "$pid" 2>/dev/null || true
      sleep 0.2
      kill -9 "$pid" 2>/dev/null || true
    fi
  done
fi

# 2) Runtime + logs (RAM; not flash)
rm -rf /var/run/nbdexport
rm -rf /var/log/nbdexport

# 3) Emhttp tree (all case variants)
rm -rf /usr/local/emhttp/plugins/NBDExport
rm -rf /usr/local/emhttp/plugins/NbdExport
rm -rf /usr/local/emhttp/plugins/nbdexport

# Stray tab pages if ever copied outside plugin dir
rm -f /usr/local/emhttp/webGui/NBD*.page /usr/local/emhttp/webGui/Nbd*.page \
      /usr/local/emhttp/plugins/dynamix/NBD*.page /usr/local/emhttp/plugins/dynamix/Nbd*.page 2>/dev/null || true
find /usr/local/emhttp/webGui /usr/local/emhttp/plugins/dynamix -maxdepth 1 -type f \
  \( -name 'NBDExport.page' -o -name 'NBDStatus.page' -o -name 'NBDHost.page' \
     -o -name 'NBDPull.page' -o -name 'NBDSettings.page' \
     -o -name 'NbdExport.page' -o -name 'NbdStatus.page' -o -name 'NbdHost.page' \
     -o -name 'NbdPull.page' -o -name 'NbdSettings.page' \) -delete 2>/dev/null || true

# 4) Flash plugin state
rm -rf /boot/config/plugins/NBDExport
rm -rf /boot/config/plugins/NbdExport
rm -rf /boot/config/plugins/nbdexport
rm -f /boot/config/plugins/nbd.plg /boot/config/plugins/nbdexport.plg /boot/config/plugins/install.plg 2>/dev/null || true

# 5) Temp install copies
rm -f /tmp/plugins/nbd.plg /tmp/plugins/nbdexport.plg /tmp/plugins/install.plg 2>/dev/null || true
rm -f /tmp/plugins/NBDExport* /tmp/plugins/NbdExport* /tmp/inline*-nbd*.sh 2>/dev/null || true

if [ -e /usr/local/emhttp/plugins/NBDExport ] || [ -e /usr/local/emhttp/plugins/nbdexport ]; then
  echo "NBD Export: WARNING — emhttp plugin path still present"
else
  echo "NBD Export: emhttp plugin paths clear"
fi
echo "NBD Export: removed."
echo "NBD Export: did not modify network.cfg, Thunderbolt Net, or FabricRouting."
echo "NBD Export: user image files under /mnt/ were left untouched."
echo "NBD Export: hard-refresh the browser."
]]></INLINE>
</FILE>

</PLUGIN>
